<!-- work_status.md — Single source of truth for what's done and what's next. Mutable: overwrite statuses in place. Default = a checklist/table; use the grid variant only when the work is combinatorial. -->

# Work Status

Status legend: `pending` / `in-progress` / `done` / `blocked`.

| Item | Status | Notes |
|---|---|---|
| <work item> | pending | |
| <work item> | pending | |

<!--
Combinatorial variant — when the work is a matrix of cases, replace the table above with a grid
(rows = component/area, columns = state/scenario, each cell a status):

| Area | scenario-A | scenario-B |
|---|---|---|
| X | done | in-progress |
| Y | pending | pending |
-->
